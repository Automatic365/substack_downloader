[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --cov=.
    --cov-report=term-missing
    --cov-report=html
markers =
    integration: Integration tests that make real network calls (deselect with '-m "not integration"')
